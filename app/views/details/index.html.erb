<h1>Ходы</h1>

<table>
  <tr>
    <th>Игрок</th>
    <th>Осталось </br> палочек</th>
    <th>Палочек </br> взято</th>
    <th>ID игры</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @details.each do |detail| %>
  <tr>
    <td><%= detail.player %></td>
    <td><%= detail.sticks %></td>
    <td><%= detail.take %></td>
    <td><%= detail.game_id %></td>
    <td><%= link_to 'Show', detail %></td>
    <td><%= link_to 'Edit', edit_detail_path(detail) %></td>
    <td><%= link_to 'Destroy', detail, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Detail', new_detail_path %>
